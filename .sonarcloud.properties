# SonarCloud automatic analysis configuration (GitHub App mode).
# pyproject.toml stays outside the analysis scope: its only finding is
# text:S8565 (lock file missing), whose remedy the fleet lockfile ban forbids.
# Automatic analysis cannot deactivate a single rule from this file, so the
# scope exclusion is the operator-authorized form (2026-09-23, bead flext-c8yle).
sonar.exclusions=pyproject.toml
