Tests:
  enforcement:
    pytest_timeout:
      allow_timeout_func_only: false
      required_configured_cap_count: 1
      timeout_owned_ini_keys:
        - addopts
        - timeout
        - timeout_func_only
  name: flext-tests
  version: "0.12.0-dev"
