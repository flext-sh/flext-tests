# Generated by flext_infra codegen — DO NOT EDIT.
# Pre-commit only orchestrates the canonical Make lifecycle; tools remain owned
# by their single Make verb.
repos:
  - repo: local
    hooks:
      - id: flext-canonical-workflow
        name: FLEXT canonical workflow
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'make setup && make fix APPLY=Y && make fmt APPLY=Y && make check && make test'
        pass_filenames: false
        always_run: true
