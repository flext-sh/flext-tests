# Generated by flext_infra codegen for flext-tests — DO NOT EDIT
repos:
  - repo: local
    hooks:
      # Why: fast fixes run before each commit; expensive gates run before push.
      # Pre-push is local operator validation and runs every BLOCKING gate: CI=Y
      # is never injected here, because it skips the lint/format/pyrefly gates
      # that must block a push (9b604d43). make test never inherits that token
      # either (mro-v4p5 forbids pytest under CI=Y).
      # A step declaring gates_skip (mro-ucxgl) narrows CHECK_GATES for the hook
      # context only: local `make check` and the `docs` verb keep the full set.
      - id: flext-pre-commit
        name: FLEXT pre-commit workflow
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'make fmt APPLY=Y && make fix APPLY=Y'
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
      - id: flext-pre-push
        name: FLEXT pre-push workflow
        language: system
        entry: >-
          bash -eu -o pipefail -c
          'unset $(git rev-parse --local-env-vars); make gen APPLY=Y && CHECK_GATES=lint,pyrefly,mypy,pyright,security,smells make check && make test WHAT=cache-clear APPLY=Y && make test WHAT=all'
        pass_filenames: false
        always_run: true
        stages: [pre-push]
